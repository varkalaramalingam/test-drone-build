kind: pipeline
type: docker
name: default
steps:
- name: build-cms-ui
  image: node
  commands:
    - ls
    - npm install
    - export NODE_OPTIONS="--max-old-space-size=4096
    - npm run build
    - ls
  when: 
    branch: 
    - master
    event:
    - push
    - pull_request
